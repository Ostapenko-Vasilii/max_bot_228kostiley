
// Runner: import the main bot entrypoint so it initializes and starts the bot.
// Avoid calling `bot.start()` here because `bot.js` already starts the bot.
import './bot.js';

// Note: To run the bot in production use `npm start` or `node bot.js`.

